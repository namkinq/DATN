@model IEnumerable<WebBanHang.Models.KhuyenMai>

@{
    ViewData["Title"] = "Voucher khuyến mại";
    Layout = "~/Areas/Admin/Views/Shared/_AdminLayout.cshtml";
}

<div class="page-header">
    <h2 class="header-title">Quản lý voucher khuyến mại</h2>
    <div class="header-sub-title">
        <nav class="breadcrumb breadcrumb-dash">
            <a asp-area="Admin" asp-controller="Home" asp-action="Index" class="breadcrumb-item"><i class="anticon anticon-home m-r-5"></i>Home</a>
            <span class="breadcrumb-item active">Voucher</span>
        </nav>
    </div>
</div>


<div class="card">
    <div class="card-body">
        <div class="row m-b-30">
            <div class="col-lg-4">
                <a class="btn btn-primary" asp-area="Admin" asp-controller="KhuyenMais" asp-action="Create">
                    <i class="anticon anticon-plus-circle m-r-5"></i>
                    <span>Thêm mới</span>
                </a>
            </div>
        </div>
        <div class="table-responsive">
            <table class="table table-hover e-commerce-table">
                <thead>
                    <tr>
                        <th>Mã</th>
                        <th>Giá trị giảm</th>
                        <th>Giá trị tối thiểu</th>
                        <th>Ngày bắt đầu</th>
                        <th>Ngày kết thúc</th>
                        <th>#</th>
                    </tr>
                </thead>
                <tbody id="records_table">
                    @if (Model != null)
                    {
                        foreach (var item in Model)
                        {
                            <tr>
                                <td>@item.Ma</td>
                                <td>@item.Gtgiam.Value.ToString("#,##0") đ</td>
                                <td>@item.GttoiThieu.Value.ToString("#,##0") đ</td>
                                <td>@item.NgayBd</td>
                                <td>@item.NgayKt</td>
                                <td>
                                    <a class="btn btn-icon btn-hover btn-sm btn-rounded" asp-area="Admin" asp-controller="KhuyenMais" asp-action="Edit" asp-route-id="@item.MaCtkm">
                                        <i class="anticon anticon-edit"></i>
                                    </a>
                                    <a class="btn btn-icon btn-hover btn-sm btn-rounded" asp-area="Admin" asp-controller="KhuyenMais" asp-action="Delete" asp-route-id="@item.MaCtkm">
                                        <i class="anticon anticon-delete"></i>
                                    </a>
                                </td>
                            </tr>
                        }
                    }

                </tbody>
            </table>
        </div>
    </div>
</div>
